server {
    listen localhost:9191;
    root www;

    max_body_size 100;

    location / {
        index index.html;
    }

    location /img/ {
        upload_path upload;
        autoindex on;
    }

    location /uploads/ {
        upload_path uploads;
        autoindex on;
        allowed_methods GET POST DELETE;
    }

    location /cgi-bin/ {
        cgi_pass;
        allowed_methods GET POST;
    }
}
